#ifndef GAME_H
#define GAME_H

#include "stdafx.h"

class game
{
public:
    // Default constructor.
    game();

    // Constructor for loading input.
    game(std::vector<std::vector<std::string> > a_input);

private:

    m_input;
    //recommendations

};

#endif